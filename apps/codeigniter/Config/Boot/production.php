<?php

declare(strict_types=1);

/**
 * CodeIgniter 4 benchmark app — production environment bootstrap.
 *
 * Boot::loadEnvironmentBootstrap() unconditionally requires
 * APPPATH/Config/Boot/{ENVIRONMENT}.php; the stock app skeleton ships this
 * file (with OPCache tuning etc.). For the benchmark nothing extra is
 * needed beyond the stock comments — keep it minimal but present.
 */